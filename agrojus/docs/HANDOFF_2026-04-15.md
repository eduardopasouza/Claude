# AgroJus — Handoff Completo para Próxima Sessão

**Data:** 2026-04-15  
**Branch:** `claude/continue-backend-dev-sVLGG`  
**Último Commit:** `bad32d5` — `feat: GIS Engine v2 + Backend Real APIs + Frontend Live Data`

---

## 1. Estado Atual da Plataforma

### Infraestrutura (Docker)
```bash
# Para subir tudo do zero:
cd c:/Users/eduar/OneDrive/Escritório/_Pessoal/AgroJus/Claude/agrojus
docker compose up -d

# Containers em operação:
# - agrojus-db-1       → PostgreSQL 15 + PostGIS (porta 5432)
# - agrojus-backend-1  → FastAPI / Uvicorn (porta 8000)

# Frontend Vite (rodar separado):
cd frontend && npm run dev   # → http://localhost:5173
```

### Banco de Dados — Estado Real
| Tabela | Registros | Fonte | Status |
|---|---|---|---|
| `environmental_alerts` (IBAMA) | **103.668** | IBAMA Embargos | ✅ Populado |
| `environmental_alerts` (MTE) | **4** | Lista Suja MTE PDF | ⚠️ Seed parcial |
| `mapbiomas_credito_rural` | **5.614.207** | MapBiomas GPKG | ✅ Populado |
| `market_quotes` | **14** | Seed manual CEPEA | ⚠️ Seed manual |

### APIs Backend — Operacionais (http://localhost:8000)
| Endpoint | Status | Dados reais |
|---|---|---|
| `GET /api/v1/market/quotes` | ✅ | Lê `market_quotes` no PostgreSQL |
| `GET /api/v1/compliance/dossier/{cpf_cnpj}` | ✅ | Lê `environmental_alerts` |
| `GET /api/v1/geo/layers/{id}/geojson` | ✅ | PostGIS ST_AsGeoJSON |
| `GET /api/v1/geo/analyze-point` | ✅ | FUNAI + INPE + IBGE |
| `POST /api/v1/consulta/completa` | ✅ | Multi-fonte |
| `GET /docs` | ✅ | Swagger UI automático |

---

## 2. GIS Engine v2 — Funcionalidades

O `frontend/src/components/GisMap.js` foi completamente reescrito:

| Funcionalidade | Como usar |
|---|---|
| **Basemap Switcher** | Select "Satélite / Terreno / OSM" no HUD |
| **Multi-layer Overlay** | Camadas se empilham sem se apagar |
| **Right-click → Análise** | Popup com Município, Risco, DETER, TI, Clima |
| **Bounding Box Search** | Shift+drag retângulo → busca desmatamento na região |
| **Coord Picker** | Click esquerdo → copia lat,lon para clipboard |
| **Legenda Dinâmica** | Cada camada tem botão X para remover individualmente |

Camadas disponíveis:
- `embargos` — 🔴 Embargos IBAMA (103k polígonos do PostGIS)
- `desmatamento` — 🔥 Alertas DETER Amazônia
- `desmatamento_cerrado` — 🔥 Alertas DETER Cerrado
- `terras_indigenas` — 🟡 FUNAI TIs
- `parcelas_financiamento` — 🟢 Crédito Rural MapBiomas (5.6M)
- `municipios` — ⬜ Malha IBGE

---

## 3. Pendências Críticas — Próxima Sessão

### P1 — ETL Lista Suja MTE (CRÍTICO)
```bash
# PDF baixado em: data/mte_trabalho_escravo.pdf
# Apenas 4 registros foram importados (parser falhou em tabelas multi-página)
docker exec agrojus-backend-1 python scripts/etl_mte_escravo.py
# Deveria carregar ~2.000+ CNPJs da lista completa
```

### P2 — Scraper CEPEA Automático (IMPORTANTE)
```bash
# market_quotes tem apenas 14 registros seed manual
# Playwright precisa de Chromium no container:
docker exec agrojus-backend-1 python scripts/fetch_cepea_playwright.py
# Fallback HTTP sem headless browser:
docker exec agrojus-backend-1 python scripts/fetch_market_prices.py
```

### P3 — Dashboard com Métricas Reais
- KPIs do dashboard ainda são hardcoded no HTML (13 fontes, etc.)
- Criar endpoint `GET /api/v1/dashboard/metrics` que retorna contagens reais
- Adicionar gráfico de tendência de preços (Chart.js)

### P4 — Login / JWT UI (IMPORTANTE)
- Backend JWT está implementado e validado
- Falta a tela de login no frontend
- `JWT_SECRET` ainda é o valor default de dev — precisa de env var

### P5 — Export PDF do Dossiê
- Motor MCR 2.9 e EUDR existem no backend
- Falta: `GET /api/v1/compliance/dossier/{cpf_cnpj}/export?format=pdf`
- Usar WeasyPrint (já está na imagem Python)

### P6 — BigQuery / Base dos Dados
- Script `etl_basedosdados.py` foi arquitetado mas requer credenciais Google Cloud
- `gcloud auth application-default login` dentro do container
- Dados disponíveis via BigQuery: PAM, PPM, CNPJ completo da Receita

---

## 4. Diagnóstico Rápido

```bash
# Verificar todas as tabelas:
docker exec agrojus-backend-1 python scripts/db_status.py

# Output esperado:
# IBAMA alerts: 103668
# MTE alerts: 4       ← Este número precisa subir para ~2000+
# Market quotes: 14   ← Este número precisa subir com o scraper CEPEA
# Credito Rural parcelas: 5614207
```

---

## 5. Credenciais

| Serviço | Valor |
|---|---|
| PostgreSQL | `localhost:5432` / db: `agrojus` / user: `agrojus` / pass: `agrojus` |
| Backend URL | `http://localhost:8000` |
| Frontend URL | `http://localhost:5173` |
| Swagger UI | `http://localhost:8000/docs` |
| Branch Git | `claude/continue-backend-dev-sVLGG` |

> ⚠️ JWT_SECRET está no valor padrão de dev. Definir via variável de ambiente antes de qualquer deploy.

---

## 6. Sequência Recomendada para Próxima Sessão

1. `docker compose up -d` — subir o ambiente
2. Corrigir ETL MTE (P1) — popular Lista Suja completa
3. Ativar scraper CEPEA (P2) — coleta de preços real
4. Dashboard métricas reais (P3)
5. Tela de login JWT (P4)
6. Export PDF Dossiê (P5)

---

*AgroJus Enterprise v0.5.0 — Handoff gerado em 2026-04-15*
