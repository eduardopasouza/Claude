# AgroJus — Handoff Completo para Próxima Sessão

**Data:** 2026-04-15 (atualizado às 03:00 BRT)
**Branch:** `claude/continue-backend-dev-sVLGG`
**Último Commit:** `4cfcb3a` — feat: dashboard expandido + roadmap v2.0 + fix queries schema

---

## 1. Estado Atual da Plataforma

### Infraestrutura (Docker)
```bash
cd "c:/Users/eduar/OneDrive/Escritório/_Pessoal/AgroJus/Claude/agrojus"
docker compose up -d

# Verificar banco:
docker exec agrojus-backend-1 python scripts/db_inventory.py

# Frontend Vite:
cd frontend && npm run dev   # → http://localhost:5173
```

### Banco de Dados — Estado Real (atualizado)
| Tabela | Registros | Fonte | Status |
|---|---|---|---|
| `environmental_alerts` (IBAMA) | **103.668** | IBAMA Embargos | ✅ |
| `environmental_alerts` (MTE) | **614** | Lista Suja MTE PDF | ✅ |
| `mapbiomas_credito_rural` | **5.614.207** | MapBiomas GPKG | ✅ |
| `mapbiomas_agriculture_cycles` | **15.180** | MapBiomas Stats | ✅ |
| `mapbiomas_coverage_states` | **953** | MapBiomas Stats | ✅ |
| `mapbiomas_irrigation_stats` | **7.174** | MapBiomas Stats | ✅ |
| `mapbiomas_mining_stats` | **1.294** | MapBiomas Stats | ✅ |
| `mapbiomas_pasture_age` | **1.911** | MapBiomas Stats | ✅ |
| `mapbiomas_pasture_vigor` | **147** | MapBiomas Stats | ✅ |
| `geo_terras_indigenas` | **655** | FUNAI WFS | ✅ |
| `geo_deter_amazonia` | **50.000** | INPE TerraBrasilis | ✅ |
| `geo_deter_cerrado` | **50.000** | INPE TerraBrasilis | ✅ **NOVO** |
| `geo_armazens_silos` | **16.676** | MapBiomas Infra | ✅ |
| `geo_frigorificos` | **207** | MapBiomas Infra | ✅ |
| `geo_rodovias_federais` | **14.255** | MapBiomas Infra | ✅ |
| `geo_ferrovias` | **2.244** | MapBiomas Infra | ✅ |
| `geo_portos` | **35** | MapBiomas Infra | ✅ |
| `market_quotes` | **24** | Yahoo Finance CBOT/CME | ✅ |
| `rural_credits` | **0** | BCB SICOR (503 manutenção) | ⏳ |
| `ana_outorgas` | **0** | ANA (ETL pronto, URL pendente) | ⏳ |

### APIs Backend — Operacionais (http://localhost:8000)
| Endpoint | Status | Observação |
|---|---|---|
| `GET /api/v1/dashboard/metrics` | ✅ | 8 KPIs + geo_summary + cotações |
| `GET /api/v1/market/quotes` | ✅ | Yahoo Finance CBOT/CME |
| `GET /api/v1/compliance/dossier/{cpf_cnpj}` | ✅ | IBAMA + MTE |
| `GET /api/v1/geo/layers/{id}/geojson` | ✅ | PostGIS, bbox filter |
| `GET /api/v1/geo/layers/desmatamento_cerrado/geojson` | ✅ **NOVO** | 50k polígonos PostGIS |
| `GET /api/v1/geo/analyze-point` | ✅ | FUNAI + INPE + NASA + IBGE |
| `GET /api/v1/geo/catalogo` | ✅ | 20+ camadas catalogadas |
| `GET /api/v1/geo/municipios/{cod}/producao` | ✅ | SIDRA/PAM |
| `GET /api/v1/geo/clima` | ✅ | NASA POWER |
| `POST /api/v1/auth/register` | ✅ | JWT + dual-mode (DB/in-memory) |
| `POST /api/v1/auth/login` | ✅ | Rate limit 5x/5min |
| `GET /api/v1/auth/me` | ✅ | Verificação de token |
| `GET /docs` | ✅ | Swagger UI |

---

## 2. O Que Foi Feito Nesta Sessão (2026-04-15)

### Fase 7 (já estava concluída, documentada aqui)
- ✅ ETL Lista Suja MTE: 614 registros
- ✅ Scraper Yahoo Finance CBOT/CME: 10 commodities em BRL
- ✅ `GET /api/v1/dashboard/metrics` operacional

### Fase 7.5 — Expansão Geoespacial e Dashboard (NOVO)
- ✅ `geo_deter_cerrado` — 50.000 alertas DETER Cerrado carregados via WFS TerraBrasilis
- ✅ Layer `desmatamento_cerrado` servida do PostGIS local (com bbox filter ST_Intersects)
- ✅ Bug corrigido: `get_session()` não é context manager — todos os handlers corrigidos
- ✅ Bug corrigido: coluna `view_date` (não `date`) na tabela `geo_deter_cerrado`
- ✅ Catálogo de camadas (`camadas.py`) expandido: 20+ fontes, status real, `ana_outorgas` nova
- ✅ Dashboard metrics expandido: DETER Amazônia+Cerrado, Terras Indígenas, armazéns, frigoríficos
- ✅ `irrigation_stats` — usa colunas de anos (`"2023"`) não `area_ha`
- ✅ ETLs criados: `etl_ana_outorgas.py`, `etl_sicor_bcb.py`
- ✅ Scripts de catálogo: `bd_catalog.py` (BasedosDados GraphQL), `catalog_data_sources.py`

### Fase 9 — Login/Auth UI (NOVO — em progresso)
- ✅ Login overlay HTML (glassmorphism, tabs Entrar/Criar Conta)
- ✅ CSS completo: `.login-overlay`, `.login-card`, `.auth-form`, `.user-badge`
- ✅ JS auth layer: `localStorage` JWT, form submit, register, logout, token verify
- ✅ User badge no sidebar com botão de logout
- ✅ Auth backend (`auth.py`) já estava completo: JWT, rate limiting, dual-mode DB/memory

### Documentação
- ✅ `docs/CONTINUIDADE_PROMPT.md` — reescrito como v2.0 com estado atual completo
- ✅ `docs/HANDOFF_2026-04-15.md` — este arquivo atualizado

---

## 3. Pendências Críticas — Próxima Sessão

### P1 — Validar Login UI no Browser ← PRÓXIMO A FAZER
- Abrir `http://localhost:5173` e testar o fluxo completo
- Registrar usuário → ver badge no sidebar → logout → login
- Se der CORS error: adicionar `http://localhost:5173` ao middleware CORS do FastAPI

### P2 — BasedosDados / BigQuery ← MÁXIMA PRIORIDADE DE DADOS
- Solicitar ao usuário: criar projeto Google Cloud gratuito
- Adicionar `GCP_PROJECT_ID=seu-projeto` ao `.env`
- Rodar: `docker exec -e PYTHONPATH=/app agrojus-backend-1 python /app/scripts/etl_basedosdados.py`
- Dados: CNPJ completo Receita Federal (2.7B+ linhas), PAM, trabalho escravo

### P3 — BCB SICOR Crédito Rural
- OData retornou 503 (manutenção) em toda esta sessão
- Arquivo pronto: `backend/scripts/etl_sicor_bcb.py`
- Tentar: `docker exec -e PYTHONPATH=/app agrojus-backend-1 python /app/scripts/etl_sicor_bcb.py`

### P4 — ANA Outorgas de Água
- ETL pronto: `backend/scripts/etl_ana_outorgas.py`
- Portal `dadosabertos.ana.gov.br` retorna 200 mas CKAN retorna 404
- Próximo passo: navegar o portal manualmente para encontrar URL do ZIP de outorgas

### P5 — Agendador de Cotações (APScheduler)
- Cotações Yahoo Finance funcionam mas são estáticas (rodadas manualmente)
- Implementar APScheduler no FastAPI: rodar `fetch_market_prices.py` às 09h e 18h BRT

### P6 — Export PDF do Dossiê (WeasyPrint)
- `GET /api/v1/compliance/dossier/{cpf_cnpj}/export?format=pdf`
- WeasyPrint disponível na imagem Python do container

### P7 — Motor de Score de Risco (0-100)
- Combinar: IBAMA (103k) + MTE (614) + DataJud + sobreposição TI (655)
- Score: Baixo / Médio / Alto / Crítico
- Exibir no dossiê e no right-click do mapa

---

## 4. Diagnóstico Rápido

```bash
# Estado completo do banco:
docker exec agrojus-backend-1 python scripts/db_inventory.py

# Dashboard metrics:
curl http://localhost:8000/api/v1/dashboard/metrics | python -m json.tool

# DETER Cerrado (50k polígonos):
curl "http://localhost:8000/api/v1/geo/layers/desmatamento_cerrado/geojson?max_features=2" | python -m json.tool

# Atualizar cotações:
docker exec -e PYTHONPATH=/app agrojus-backend-1 python /app/scripts/fetch_market_prices.py

# Testar SICOR (se BCB voltou):
docker exec -e PYTHONPATH=/app agrojus-backend-1 python /app/scripts/etl_sicor_bcb.py

# Registrar usuário (checar login):
curl -X POST http://localhost:8000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@agrojus.br","password":"agrojus123","name":"Admin","cpf_cnpj":"","plan":"enterprise"}'
```

---

## 5. Arquivos Chave

| Arquivo | Função |
|---|---|
| `backend/app/api/geo.py` | Rotas GIS — camadas PostGIS + WFS |
| `backend/app/api/dashboard.py` | Métricas KPI + geo_summary |
| `backend/app/api/auth.py` | Login/registro JWT (completo) |
| `backend/app/collectors/camadas.py` | Catálogo 20+ camadas |
| `backend/scripts/etl_deter_cerrado.py` | ETL DETER Cerrado WFS → PostGIS |
| `backend/scripts/etl_ana_outorgas.py` | ETL ANA Outorgas (URL pendente) |
| `backend/scripts/etl_sicor_bcb.py` | ETL BCB SICOR (BCB em manutenção) |
| `backend/scripts/etl_basedosdados.py` | BigQuery (requer GCP_PROJECT_ID) |
| `backend/scripts/fetch_market_prices.py` | Yahoo Finance 10 commodities |
| `frontend/index.html` | Login overlay + user badge (NOVO) |
| `frontend/style.css` | CSS login + badge |
| `docs/CONTINUIDADE_PROMPT.md` | Roadmap v2.0 do projeto |

---

## 6. Credenciais

| Serviço | Valor |
|---|---|
| PostgreSQL | `localhost:5432` / db: `agrojus` / user: `agrojus` / pass: `agrojus` |
| Backend URL | `http://localhost:8000` |
| Frontend URL | `http://localhost:5173` |
| Swagger UI | `http://localhost:8000/docs` |
| Branch Git | `claude/continue-backend-dev-sVLGG` |

> ⚠️ **JWT_SECRET** ainda é o valor padrão de dev — adicionar ao `.env` antes de qualquer deploy.
> ⚠️ **GCP_PROJECT_ID** não configurado — bloqueia BasedosDados (BigQuery).

---

*AgroJus Enterprise v0.7.0 — Handoff atualizado em 2026-04-15 às 03:00 BRT*
*(Fase 7 ✅ + Fase 7.5 geo ✅ + Fase 9 login UI ⚠️ em progresso)*
